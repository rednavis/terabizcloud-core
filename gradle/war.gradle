apply plugin: "war"

bootWar {
    mainClassName = "com.terabiz.core.CoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
