apply plugin: "war"

bootWar {
    mainClassName = "com.terabizcloud.core.CoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
